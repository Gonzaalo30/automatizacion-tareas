name: Ejecutar Automatizaciones

on:
  schedule:
    - cron: '0 7 * * 1'
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - run: python scripts/resumen_tareas_lunes.py
      - run: echo "SEO"
      - run: echo "SEO Local"
      - run: echo "IA"
